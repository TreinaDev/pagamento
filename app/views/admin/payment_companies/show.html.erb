<div class="container">
  <h2 class="text-center"><%= @payment_company.name%></h2>
  <p> 
    <%= ClientProfile.human_attribute_name(:image) %>
    <%= image_tag(@payment_company.image, size: "100x80") %>
  </p>
  <p> 
    <%= ClientProfile.human_attribute_name(:payment_method_id) %>
    <%= @payment_company.payment_method.name%>
  </p>
</div>